[pytest]
filterwarnings =
    ignore::DeprecationWarning:_pytest.assertion.rewrite
log_cli = true
log_cli_level = INFO
log_cli_format = %(asctime)s [%(levelname)s] %(name)s: %(message)s
markers =
    server_args(args): customize echo_server startup flags
